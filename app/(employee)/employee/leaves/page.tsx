import { EmployeeLeaves } from "@/components/dashboard/employee/employee-leaves"

/**
 * EmployeeLeavesPage Component
 * 
 * Renders the employee leaves management page in the dashboard.
 * Allows viewing and requesting leaves.
 * 
 * @returns {JSX.Element} The employee leaves page
 */
export default function EmployeeLeavesPage() {
  return <EmployeeLeaves />
}
