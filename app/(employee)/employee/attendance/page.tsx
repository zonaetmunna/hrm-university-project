import { EmployeeAttendance } from "@/components/dashboard/employee/employee-attendance";

/**
 * EmployeeAttendancePage Component
 * 
 * Renders the employee attendance tracking page in the dashboard.
 * Allows viewing attendance history and recording daily check-in/check-out.
 * 
 * @returns {JSX.Element} The employee attendance page
 */
export default function EmployeeAttendancePage() {
  return <EmployeeAttendance />
}
